command:
  - "uvicorn"
  - "main:app"
  - "--host"
  - "0.0.0.0"
  - "--port"
  - "8000"
env:
  # ORCHESTRATOR_ENDPOINT is injected by the deploy pipeline at app-create time.
  - name: "ORCHESTRATOR_ENDPOINT"
    value: ""
